import { outerBlock0371 } from './component.js.js';

export default function save(props) {
	const { attributes } = props;
	return (
		<outerBlock0371
			clientId={attributes.clientId}
			attributes={attributes}
			for_={'save'}
		/>
	);
}
