import { ColumnResponsive0574 } from './component';

export default function save({ attributes, className }) {
	return (
		<ColumnResponsive0574
			attributes={attributes}
			className={className}
			for_={'save'}
		/>
	);
}
