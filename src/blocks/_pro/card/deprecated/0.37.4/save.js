import { Component0374 } from './component';

export default function save({ attributes, className }) {
	return (
		<Component0374
			attributes={attributes}
			className={className}
			for_={'save'}
		/>
	);
}
