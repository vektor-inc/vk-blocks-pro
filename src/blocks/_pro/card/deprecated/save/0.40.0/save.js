import { Component0400 } from './component';

export default function save({ attributes, className }) {
	return (
		<Component0400
			attributes={attributes}
			className={className}
			for_={'save'}
		/>
	);
}
