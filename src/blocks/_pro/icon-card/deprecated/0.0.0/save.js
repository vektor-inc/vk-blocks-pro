import { DepCompo01 } from './component';

export default function save({ attributes, className }) {
	return (
		<DepCompo01
			attributes={attributes}
			className={className}
			for_={'save'}
		/>
	);
}
