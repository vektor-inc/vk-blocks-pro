{
	"apiVersion": 2,
	"name": "vk-blocks/animation",
	"category": "vk-blocks-cat",
	"title": "Animation",
	"attributes": {
		"effect": {
			"type": "string",
			"default": "slide-up"
		},
		"speed": {
			"type": "string",
			"default": "fast"
		},
		"range": {
			"type": "string",
			"default": "short"
		},
		"clientId": {
			"type": "string",
			"default": ""
		},
		"blockId": {
			"type": "string",
			"default": "1"
		}
	},
	"description": "Add animation to elements when scrolling the page.",
	"textdomain": "vk-blocks",
	"supports": {
		"className": true
	}
}
